import styled from "styled-components";
import * as common from "../../../styles/common";

export let fullInfoCard = styled(common.flexColumn)`
  background: white;
`;

export let buttons = styled(common.flexRow)``;

export const h1 = styled.h1`
  font-size: 26px;
  font-weight: 700;
`;

export const h3 = styled.h3`
  font-size: 18px;
  display: block;
  text-align: center;
`;